<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Enable instant app support -->
    <dist:module dist:instant="true" />

    <!-- Declare features -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- Declare permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_android_background_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_android_background_24dp"
        android:supportsRtl="true"
        android:taskAffinity=""
        android:theme="@style/Theme.GestureRecognizer"
        tools:ignore="AllowBackup">

        <activity android:name=".kotlin.MainActivity" />
        <activity android:name=".java.Registration.ForgetPasswordActivity" />
        <activity android:name=".java.Registration.Home" />
        <activity android:name=".java.Registration.SignUp" />
        <activity
            android:name=".java.Registration.profile.Profile"
            android:parentActivityName=".java.Registration.Home"
            android:theme="@style/Theme.MaterialComponents.Light"
            android:label=""/>

        <activity android:name=".java.Registration.profile.UpdateProfileActivity" />
        <activity android:name=".java.Registration.profile.UploadProfilePicActivity" />
        <activity android:name=".java.Registration.profile.ChangePasswordActivity" />
        <activity
            android:name=".java.Registration.SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Declare notch support -->
            <meta-data
                android:name="android.notch_support"
                android:value="true" />
        </activity>
    </application>

</manifest>
